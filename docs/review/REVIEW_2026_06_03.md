# physgate — Comprehensive Code Review (2026-06-03)

**Status: AUTHORITATIVE.** 20 agents, 593 tool calls, 7 dimensions, adversarial
cross-verification of all CRITICAL/HIGH findings. This is the work order for the
next rebuild pass. Every item below must be resolved to reach A+ across all dimensions.

---

## Overall: B+ (engineering ahead of science)

| Dimension | Grade | Target | Gap |
|---|---|---|---|
| Architecture | B+ | A+ | MockWorldBackend fidelity, single-scene hardcoding, MCP thin |
| Correctness | B+ | A+ | Audit trail, silent degradation, Scene mutability |
| Experimental | B | A+ | **2 fatal flaws** + sample size + difficulty + D5 gap |
| Security | B | A+ | Prompt injection, audit auto-verify, feedback sanitization |
| Testing | A- | A+ | Viz deps, timing fragility, eval v2 real-LLM gap, Scene mutation |
| Code Quality | A- | A+ | Dead code, broad except, missing __all__, JSONL hack |
| Documentation | A- | A+ | Stale refs, pyproject desc, architecture.md phantom components |

---

## FIXES REQUIRED (all of them — no skipping)

### FATAL (blocks any peer review)

**F1. E1 ablation uses symbolic surrogate, not actual Isaac physics**
- File: `physgate/eval_v2/ablation.py:164-179`
- `_select_plans_a3` uses `symbolic_l2` + `MockWorldBackend` — NOT Isaac Sim
- README table says "Physics gate" but no GPU simulation ever runs in E1
- FIX: Add an `A3_physics` condition that runs actual Isaac L2 rollouts (even on
  a 5-instance subset). Keep the symbolic A3 as `A3_nav_symbolic` for comparison.
  Update README table labels to distinguish. If Isaac is too expensive for all 20
  instances, run a subset and report both.

**F2. Confidence intervals inflated ~4.5x**
- File: `physgate/eval_v2/ablation.py` (Wilson CI computation)
- A0: n=420 (20 instances × 21 plans) but outcomes are deterministic per plan —
  effective independent sample = n=20. Reported CI [0.38, 0.48], correct ≈ [0.24, 0.63].
- FIX: Compute CIs at instance level. For each instance compute expected success
  rate over its plan pool, then Wilson CI over 20 instance-level rates. Report both
  instance-level and plan-level breakdowns. Add a footnote explaining the methodology.

### HIGH (blocks A+ in their dimension)

**H1. D5 unreachable-goal defect class missing from E2 corpus**
- File: `physgate/eval_v2/defects.py:20-28`, lines 145-152
- EVALUATION_METHODOLOGY.md defines D5 but code has no D5 injector
- This is the ONE defect where physics uniquely outperforms symbolic
- FIX: Implement D5 injector (procedurally enclose target with obstacles).
  Add test cases. Re-run E2. The symbolic gate should now score recall<1.0 on D5
  while physics gate scores 1.0, breaking the current identical-F1 tie.

**H2. Orchestration eval scores 100% with zero variance = insufficient difficulty**
- 7 scenarios, 3 repeats, all 100% — uninformative
- FIX: Add harder scenarios: multi-step tasks with ambiguous instructions, scenes
  requiring replanning after skill failure, adversarial object placements,
  conflicting preconditions, tasks requiring the agent to recognize partial
  infeasibility. Target: at least 2-3 scenarios where orchestration metrics < 100%.

**H3. Sample size far below cited standards**
- n=20 feasible + n=8 infeasible vs SafeAgentBench 750 / PlanBench 600
- FIX: Use procedural generator to scale to n≥50 feasible + n≥20 infeasible.
  Report improvement. Acknowledge remaining gap honestly.

**H4. synthesize_base_trajectory silently degrades unknown targets**
- File: `physgate/gate/trajectory.py:159-162`
- Hold-in-place instead of raising error. D-016 fix applied to compile_mission
  but NOT here. Dead except in caller (l2_physics.py:99-101).
- FIX: Replace hold+continue with `raise UnknownTargetError(...)`. Remove the
  dead except or keep it as a proper handler.

**H5. Audit trail verify_integrity gap**
- File: `physgate/audit/trail.py:91-101`
- Records after last checkpoint unprotected. No contiguity check. _records is
  a public mutable list.
- FIX: (a) In close(), call checkpoint() to flush remaining records before
  final verify. (b) Add coverage check: verify checkpoints span [0, len(_records)).
  (c) Auto-call verify_integrity() at pipeline completion (graph.py / fetch_and_place).
  (d) Make _records private with a read-only view property.

**H6. Scene Pydantic model is mutable despite immutability claims**
- File: `physgate/gate/schemas.py:72` — plain BaseModel, no frozen
- test_l3_scene.py:44 and :60 directly mutate scene.gripper_empty = False
- FIX: Add `model_config = ConfigDict(frozen=True)` to Scene. Fix tests to use
  `scene.model_copy(update={"gripper_empty": False})` instead of direct mutation.

**H7. LLM prompt injection via unsanitized feedback loop**
- File: `physgate/planner/planner.py:182-186` — feedback string f-string injected
- File: `physgate/orchestrator/graph.py:167,212-214` — gate rationale and execution
  errors flow into feedback unsanitized
- FIX: (a) Truncate feedback to max length. (b) Use structured Pydantic model for
  feedback instead of raw string. (c) Strip or escape any prompt-like patterns.
  (d) Sanitize the `task` string at planner.py:177 the same way.

### MEDIUM (blocks A+ polish)

**M1. pyproject.toml description still says "best-of-N"**
- FIX: Update description to reflect post-rebuild framing.

**M2. architecture.md has phantom components**
- Claims PostgresSaver for MVP (code uses MemorySaver)
- Describes RL fallback and Monitor component (neither exists)
- ROS 2 Humble in requirements table (not used)
- SUPERSEDED banner only on §6, should be at document top
- FIX: Add top-level SUPERSEDED banner pointing to REBUILD.md. Remove or mark
  phantom components as "PLANNED / NOT IMPLEMENTED". Fix PostgresSaver → MemorySaver.

**M3. JSONL string manipulation hack**
- File: `physgate/audit/trail.py:113`
- `.replace('{', ...)` string hack
- FIX: Use proper dict construction + json.dumps().

**M4. Missing __all__ exports in subpackages**
- Most __init__.py files lack __all__
- FIX: Add __all__ to every public subpackage __init__.py.

**M5. Broad Exception catches**
- Scattered bare `except Exception` without specific types
- FIX: Replace with specific exception types. Log context if catching broadly.

**M6. Dead backward-compatibility aliases**
- FIX: Find and remove. No deprecation shims needed for a pre-alpha project.

**M7. Duplicated fake Anthropic client in tests**
- Multiple test files define the same fake
- FIX: Extract to tests/conftest.py or tests/fakes.py shared fixture.

**M8. `Any` type annotations on client/checkpointer**
- FIX: Replace with Protocol types.

**M9. matplotlib/Pillow not in dev deps**
- Viz tests silently skipped, reducing actual coverage
- FIX: Add matplotlib and Pillow to [dev] dependencies.

**M10. L1 latency test timing-fragile**
- FIX: Use relative threshold or mark with pytest tolerance/xfail for CI.

**M11. No CONTRIBUTING.md**
- FIX: Add with venv setup, test instructions, Isaac integration guidance.

**M12. No LLM cost bounds in orchestrator**
- FIX: Add token budget cap to prevent runaway LLM calls.

**M13. E1 needs n≥50 feasible instances**
- Use procedural generation. Report improvement over current n=20.

**M14. Real-LLM eval gap**
- eval v2 only tested against MockPlanner
- FIX: Run at least one full eval pass with real Claude. Record and compare.

**M15. Single-scene limitation not explicitly acknowledged in README**
- FIX: Add honest "Current Limitations" section in README.

---

## VERIFICATION CHECKLIST (run after all fixes)

- [ ] `pip install -e ".[dev]" && pytest` passes with 0 failures, 0 errors
- [ ] No test silently skipped that should run (especially viz tests)
- [ ] E1 ablation has real Isaac condition — README table labels honest
- [ ] CIs recomputed at instance level — widths plausible for n=20
- [ ] D5 defect implemented — E2 shows physics gate > symbolic gate on D5
- [ ] At least 2 orchestration scenarios score < 100%
- [ ] Scene model is frozen — direct mutation fails at type-check level
- [ ] trajectory.py raises on unknown targets — no silent degradation
- [ ] Audit verify_integrity auto-called at pipeline end
- [ ] Feedback loop sanitized — structured Pydantic, truncated, no raw f-string
- [ ] pyproject.toml description updated
- [ ] architecture.md cleaned (no phantom components)
- [ ] All __init__.py have __all__
- [ ] No broad Exception catches
- [ ] CONTRIBUTING.md exists
- [ ] README has "Current Limitations" section
- [ ] All markdown renders correctly (no broken links/images)
